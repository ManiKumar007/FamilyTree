# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here

# Google OAuth (Optional - for Google Sign-In)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_WEB_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# API Configuration
# Backend API URL (use localhost for development)
API_BASE_URL=http://localhost:3000/api

# App URL (use localhost for development)
APP_URL=http://localhost:5500

# Network Configuration (Android Development Only)
# Replace with your local machine's IP if testing on real Android device
# Not needed for web or emulator (which uses localhost)
# HOST_IP=192.168.1.100